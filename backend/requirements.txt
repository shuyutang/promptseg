# NOTE: torch/torchvision are installed separately (CUDA wheel index) -- see Dockerfile.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
python-multipart>=0.0.9

numpy
Pillow

# SAM 2.1 lives in transformers >= 5.0 as Sam2Model / Sam2Processor
transformers>=5.0
accelerate
safetensors

# DICOM
pydicom>=3.0
pylibjpeg>=2.0
pylibjpeg-libjpeg>=2.0
pylibjpeg-openjpeg>=2.3
pylibjpeg-rle>=1.3

# tests
pytest
httpx
